# Frontend

Next.js dashboard for side-by-side comparison of:

- LLM-only
- Basic RAG
- TigerGraph GraphRAG

The UI is designed around the hackathon requirement that a single query should run all three pipelines and present answers plus core metrics together.
