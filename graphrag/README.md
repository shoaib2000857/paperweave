# GraphRAG

This directory is reserved for the official TigerGraph GraphRAG deployment and local customization layer.

Recommended workflow:

1. Clone the official TigerGraph GraphRAG repo here.
2. Use `configs/graphrag_server_config.template.json` as a starting point for `configs/server_config.json`.
3. Deploy the official stack and point PaperWeave to its API.
